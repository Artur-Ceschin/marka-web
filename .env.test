# Loaded by Vitest (mode=test). Fake but well-formed Cognito values, so the
# Google button renders and the PKCE flow can be exercised without a real pool.
VITE_API_URL=https://api.markaplant.app
VITE_COGNITO_DOMAIN=marka-auth-dev.auth.us-east-1.amazoncognito.com
VITE_COGNITO_CLIENT_ID=test-client-id
VITE_OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback
