# Dev server only (`pnpm dev`). API calls go through the Vite proxy in
# vite.config.ts, so the SameSite=Strict refresh cookie is same-origin here, as
# it is in production. Everything else comes from .env.
VITE_API_URL=http://localhost:3000/api
