# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage
coverage
.claude
.agents

# pnpm's content-addressable store. The Amplify build puts it here so the
# build cache can keep it between deploys; it holds tens of thousands of
# files and must never be linted, formatted or committed.
.pnpm-store

# Local overrides
.env.local
.env
